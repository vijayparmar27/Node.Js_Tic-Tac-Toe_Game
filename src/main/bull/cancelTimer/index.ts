import { turnTimerCancel } from "./turnTimer.cancel";
import { rejoinTimerCancel } from "./rejoinTimer.cancel";

const cancelProcess = {
    turnTimerCancel,
    rejoinTimerCancel
}

export default cancelProcess;